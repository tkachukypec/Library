<Window x:Class="LibraryIS.AddPublicationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryIS"
        mc:Ignorable="d"
        Title="{Binding Title,StringFormat={}{0} книгу}" Height="450" Width="800">
    <Grid>
        <TextBlock Text="{Binding Title,StringFormat={}{0} книгу}" FontWeight="Bold" FontSize="20" Margin="40,0,-0.6,-0.8"></TextBlock>
        <local:PhotoPicker SaveDirectory="images/books" ImagePath="{Binding Publication.Image,Mode=TwoWay}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="40" Width="150" Height="150"></local:PhotoPicker>
        <StackPanel HorizontalAlignment="Right" Margin="0,40,20,0" Orientation="Vertical">
            <TextBlock Text="Название книги"></TextBlock>
            <TextBox Width="220" Text="{Binding Publication.Book.Title}"></TextBox>


            <TextBlock Text="Описание"></TextBlock>
            <TextBox Width="220" Text="{Binding Publication.Book.Description}"></TextBox>

            <TextBlock Text="Год издания"></TextBlock>
            <TextBox HorizontalAlignment="Left" Width="70" Text="{Binding Book.Year}"

        </StackPanel>
    </Grid>
</Window>
